import React from 'react';

// This component is for defining what components 
// we want to test in the Playground.
// Replace `null` with the component that you want to test!
// Make sure to enable the feature flag in FeatureFlags.js...
export const PlaygroundComponent = () => (null);